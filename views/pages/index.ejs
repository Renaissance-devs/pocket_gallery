<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../layouts/head') %>
  <link rel="shortcut icon" href="/assets/painting.png" type="image/x-icon">
  <title>My Gallery</title>
</head>

<body>
    <%- include('../layouts/header') %>
  <main>
    <h4>Saved Works (<%= count %>)</h4>
    <h3>Select Gallery to View</h3>
    <form action="" method="">
      <select id="gallery-select" name="gallery">
        <option value="*">All Galleries</option>
        <% galleries.forEach(gallery => { %>
        <option value="<%= gallery.gallery %>"><%= gallery.gallery %></option>
        <%})%>
      </select>
      <button type="submit">View Gallery</button>
    </form>

    </select>
    <ul>
      <% result.forEach(art => { %>
      <li class="art-items" data-work-id="<%= art.id %>">
        <section class="art-container detail-view">
          <img src="<%= art.image_url %>" alt="image of artwork">
          <h3><%= art.title %></h3>
          <span><%= art.artist %></span>
          <p>
            <% if (art.gallery) { %>
            Gallery:
            <%= art.gallery %>
            <% } %>
          </p>
          <p class="detail-button"><a id="details-a" href="/works/<%= art.id %>">View Details</a></p>
        </section>
      </li>
      <% }) %>
    </ul>
  </main>
  <%- include('../layouts/footer') %>
</body>
<script
  src="https://code.jquery.com/jquery-3.4.1.min.js"
  integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo="
  crossorigin="anonymous"></script>
<script src="/js/app.js"></script>
</html>